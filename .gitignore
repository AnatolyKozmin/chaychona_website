.DS_Store
.env
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/

backend/.venv/
backend/.vendor/
frontend/node_modules/
frontend/dist/

# Хранилище загруженных медиа (фото блюд, озвучка, снимки из чек-листов).
# Наполняется в рантайме и монтируется томом — в репозитории ему не место.
backend/media_uploads/

# Локальный источник для разовой заливки блюд (не в репозиторий, ~200 МБ)
/audio/
/dish_photos/
/images/
/ЖУ_Вкусная_тетрадь_реестр.xlsx
